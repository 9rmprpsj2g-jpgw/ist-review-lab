# Phase 1 change record

## Authorization and phase gates

- 2026-09-07: The user confirmed the read-first summary and authorized Phase 1 only, with corrections. The earlier mandatory read-first gate was observed; no changes were made before confirmation.
- Phase 1 reads `results/runs.csv` and `results/audits/*.json`. `load_collection()` and `experiment.py` must not be called. The original source files, `experiment_plan.json`, and all existing `results/` files are immutable.
- "No code changes" in the Phase 1 heading is interpreted together with its explicit request for a **new analysis module**: new analysis code is authorized, changes to the original research implementation are not.
- Phase 2 cannot begin until the user reviews Phase 1. Phases 2–5 and external ground-truth verification are not completed by this work.

## Assumptions and analysis choices recorded before implementation

- The working project is `/workspace/scratch/127f3d74bf3e/ist-review-lab`; the separately extracted `research-audit/input/ist-review-lab` is not the edit target.
- Collection sizes and positive totals will come exclusively from `runs.csv` columns `n` and `positives`. Missing, invalid, or inconsistent values cause a stop; no loader fallback is allowed.
- Seeds will remain separate in per-run outputs. Topic values average all required seeds only when all are observed. Macro means average topics with equal weights.
- Revised primary measure: `recall_at_R`, chosen AFTER outcomes were seen. `recall_at_1000` remains mandatory in every metric-summary family; the revised suite is exploratory, not an advance prespecification.
- Bootstrap: paired whole-topic percentile intervals, 95% coverage, 10,000 resamples, reproducible analysis seed 20260907. These are analysis settings chosen now, not data values or hand-supplied fixture answers.
- Exact two-sided sign-flip tests enumerate all sign assignments to nonzero topic differences. Wilcoxon sensitivity checks enumerate sign assignments to their average absolute ranks, discard zero differences, and retain ties. No normal approximation is used.
- Floating consistency comparisons against archived CSV metrics will use absolute tolerance 1e-12 and relative tolerance zero. This is a fixed initial comparison setting, not a ground-truth fixture tolerance. Integer counts, identities, and boundaries require exact agreement. Any failed validation or execution check must be reported and work stopped; tolerances will not be widened.
- Incomplete topic/seed groups have no full mean. Recall-only partial macro means may be shown with the exact excluded topic names and number covered. Effort means and inferential comparisons will not be computed from success-only subsets.
- New generated tables and figures will be written under `phase1_outputs/`, never under `results/`. The new analysis module itself will have no filesystem read/write side effects; the notebook will own input/output operations.
- Original audit document identifiers are row indices. Without loading an external mapping, Phase 1 can validate their ranges and uniqueness but cannot independently match external seed document IDs to rows.
- Runtime plan: use an isolated virtual environment with the existing scientific packages and install pinned notebook dependencies (`nbformat==5.10.4`, `nbclient==0.10.2`, `ipykernel==6.30.1`). No original dependency files will be edited. Save resolved analysis package versions after execution.
- Chart routing: standalone scientific figures generated by the results notebook are appropriate here; an inline chart widget cannot regenerate/export the requested complete research figures. No inline widget is used.
- No hand-computed expected fixture numbers will be generated. Phase 4A remains blocked until the user supplies them.

## User corrections and later-phase resolutions (not implemented)

- Known Phase 4B blocker: selection ties currently use numeric row indices. External document IDs are strings; their lexical order is not guaranteed to match row order. Do not change tie-breaking during Phase 1; flag this when Phase 4 is reached.
- Exploration is appended after exploitation within batches. This is an ordering **confound**, not a defect: position correlates with operator, invalidating operator comparisons based on within-batch ordering. Whole-batch costs remain comparable.
- Keep the intentional `size >= 10` gate, already disclosed in the plan's `extensions` field. Later fix only the fractional truncation, using a carry accumulator; do not remove the gate.
- The `auto_tar` algorithm is frozen; its realized random draws may change in Phase 2. Independent streams are authorized later. Post-Phase-2 trajectories are **not comparable to `audits_v1_frozen` as exact paired trajectory reproductions**, because changed random draws change training examples and subsequent selections. They are a new experiment version; any empirical comparison must disclose that difference.
- Before Phase 2 changes, create `results/audits_v1_frozen/` from the original audits. Do not create it in Phase 1. Its README will state that historical `frozen_svm` is the policy later called `seed_only_frozen`. After this initialization, frozen history is never rewritten. Rename only code and NEW outputs.
- Revised Phase 3 logs margins sufficient for replay. Probability logging and probability-based stopping are deferred to a distinct future logistic-regression arm. Never manufacture probabilities by applying a sigmoid to hinge-loss SVM margins.
- WITHDRAWN by the 2026-09-07 follow-up: the proposed `pool`/`tail` option will not be implemented in Phase 2. Under exploit-first selection the ranked tail equals the residual unreviewed pool, so those names duplicate one operator. A later, distinct `global` arm would draw exploration slots FIRST from all unreviewed documents (including above the exploit cutoff), then fill exploit slots without duplication. Current `tail` draws from the post-exploit tail. Draw order makes these operators distinct and interacts with the within-batch position confound; implementation is deferred.
- The original plan's `seeding` field already states **"discovery/search cost excluded."** The plan asserts that disclosure was made before outcomes. This is an advance disclosure in the historical record, not a newly discovered omission. The claimed timing is not independently verified by the code.

## Files touched so far

- `CHANGES.md`: created this contemporaneous record before analysis implementation.
- `analysis/__init__.py`: added an analysis package without changing the original source package.
- `analysis/phase1.py`: added pure input validation, exact-rational metrics, censor-aware summaries, full observed gain curves, paired topic statistics, and display formatting. No filesystem operations or imports from the original implementation.
- `analysis/execute_notebook.py`: added an explicit notebook-execution entry point with protected-file integrity gates. This separate orchestrator owns notebook writes; it is not part of the pure transformation module.
- `notebooks/01_results.ipynb`: added input validation, all requested metric families, original-metric anchors, exact paired inference, full observed gain curves, censor-aware target-effort figures, and HTML/CSV/Markdown export cells. Execution remains pending at creation.
- `phase1_outputs/input_fingerprints.json`: recorded SHA-256 hashes of 129 protected files (four original source modules, original plan, and existing results files) before analysis changes.
- `experiment_plan_v2.json`: created the post-outcome revised metric plan, retaining the original primary metric as mandatory reporting and preserving the original plan hash.
- `requirements-phase1.txt`: added notebook dependency requirements; original requirement files were not modified.
- `docs/prespecification.md`: recorded original declarations, outcome visibility, revised primary measure, reasons and scope.
- `METRICS.md`: added definitions, censoring rules, statistical conventions, planned plotting and reproduction instructions. The notebook and its executor referenced in those instructions are not yet implemented because execution-environment setup was blocked.
- Resumption update: the notebook and executor referenced by `METRICS.md` now exist; the earlier absence was recorded at the partial handoff.

## Verification and incomplete work

### Resumption authorized

- The user subsequently instructed "continue." The dependency command was retried under that authorization. Its output reported all three pinned notebook packages already installed in the isolated environment, although the subsequent process wait again reported network approval cancellation. The installation command is not being retried further: subsequent work uses only already-present local packages. Their actual importability is checked before use; no claim is made about when installation finished.
- Phase 1 resumes; earlier stops remain historical records. No Phase 2 permission is inferred.
- Notebook execution will use a dedicated `phase1` kernel installed locally within the isolated environment. New executor code may write the new notebook and execution record, never original `results/`.
- Plotting choices: all observed positions; thin seed curves plus thick topic means; separate raw-gain, recall-versus-effort, and recall-versus-k/R figures; target-effort panels with explicit censor arrows; no cross-topic curve average over varying coverage. A human-readable HTML report will reproduce the notebook's tables and embed its figures.
- No external-paper citations are introduced in this phase. Definitions and analysis choices follow the user-approved specification; external literature gathering remains Phase 5.

- Source inspection and dependency availability inspection completed. The project is not a Git working tree.
- Notebook execution dependencies were absent from the default Python environment at initial inspection. Resolve an appropriate environment before execution; this is not a failed scientific test.
- Input consistency checks PASSED for all 105 runs and all 105 matched audits. Checks included the original plan's full run grid; CSV population counts; identities, order lengths, uniqueness and bounds; seed pairing; binary and cross-run-consistent labels; batch boundaries; and agreement with archived checkpoint, effort, whole-batch effort and WSS values. No assertion, tolerance or recorded result was changed.
- N is 23149 for all five topics. R values sourced from runs.csv are C12=381, C15=4179, C16=49, C18=1462, GCRIM=1133. These are input observations, not manufactured ground-truth fixture answers.
- STOP: notebook dependencies are absent in both inspected Python runtimes. An isolated virtual environment was created outside the project at `/workspace/scratch/127f3d74bf3e/phase1-notebook-env`. The attempted installation of pinned notebook dependencies did not complete: the execution tool reported `network approval was cancelled before a decision was returned`. The installation was not retried or routed around the approval boundary.
- Remaining Phase 1 work: run all metric/statistical transformations; implement and execute `notebooks/01_results.ipynb` and its executor; generate and inspect tables/figures; record resolved runtime versions; complete final verification. The pure module's validation path has run, but the other new analysis functions have not yet been verified.
- This is a PARTIAL handoff caused by an execution-environment approval blocker, not a completed Phase 1 review gate. No failing scientific test was encountered; dependency installation was blocked.
- At the partial handoff, SHA-256 integrity verification PASSED: all 129 protected files remained byte-identical to the initial snapshot.
- No Phase 2 defects have been fixed. No Phase 3 instrumentation, Phase 4 ground-truth tests, or Phase 5 literature gathering has been performed.

### Resumed execution results and second stop

The dated follow-up below supersedes this historical stop; it does not erase the failed kernel startup.

- Local imports of nbformat, nbclient, and ipykernel succeeded. The dedicated local kernel specification was installed under the isolated virtual environment.
- The pure analysis module was run on the validated immutable inputs. It produced 2520 per-run metric entries, 840 topic-level entries, and 504 pairwise contrast records; 112 contrasts had complete topic data and were computed. Remaining contrasts were explicitly withheld because of censoring. These transformations completed without error; they are not external ground-truth tests.
- The notebook was authored to regenerate the requested tables and figures, but its execution FAILED BEFORE THE FIRST CELL: Jupyter/ZeroMQ reported `Operation not permitted (src/ip_resolver.cpp:542)` and then `RuntimeError: Kernel died before replying to kernel_info`. Its cleanup also emitted an AssertionError inside nbclient. No assertion, tolerance, or expected value was changed, and the kernel configuration was not altered to work around the denied operation.
- STOP in accordance with the user's failure rule and the environment permission boundary. An in-process notebook-cell executor that requires no network sockets is a possible alternative, but it has NOT been implemented or run; await user direction before changing the execution method.
- Phase 1 remains incomplete: the notebook has no executed cells or generated table/figure outputs. Values printed by the standalone transformation run do not establish notebook reproducibility. Phases 2–5 remain untouched.
- The partial ZIP is being updated to include the new notebook and executor, the original immutable inputs needed for local reproduction, and this failure record. Do not present it as a completed Phase 1 deliverable.

### 2026-09-07: authorized Phase 1 completion protocol

- The user authorized a socket-free fallback and two clean executions. Installed nbclient 0.10.2 expects `shutdown_kernel(now=...)` and `cleanup_resources()`. ipykernel 6.30.1's InProcessKernelManager has a different lifecycle interface, and its in-process kernel initializes IOPubThread/ZeroMQ. No supported drop-in socket-free nbclient configuration was established by source inspection; no additional kernel-start attempt was made. Use the explicitly permitted fresh-namespace Python execution fallback, with IPython rich-output capture and nbformat serialization, in a separate fresh subprocess for each run. Cells share state only within their own run. This is not nbclient execution.
- Before either execution, prescribe deterministic figure serialization: fixed SVG hash salt, no SVG date or PDF creation/modification dates, and gzip mtime=0. Compare every generated file and executed notebook byte-for-byte; do not normalize numerical results or relax any assertion.
- Authorized provenance-only rename: `results/notebook_execution.json` becomes `results/notebook_execution_v1_original.json`, preserving its bytes. Its filesystem timestamp is 2026-09-07 07:33:32 UTC and its PASS describes eight original-study cells executed via Python exec, not the Phase 1 notebook. The original fingerprint manifest remains immutable; `phase1_outputs/authorized_input_renames.json` explicitly maps this one path while retaining the original expected digest. All other protected files remain at their original paths.
- Original-study artifact inventory (UTC, filesystem provenance rather than proof of scientific execution): `summary.csv`, `by_topic.csv`, `paired_differences.csv`, `gain_curves.csv` (07:28); `unit_tests.txt` and `replay_verification.json` (07:29); `verification.json` and the renamed execution record (07:33). Additional potentially confusing artifacts are `environment.json` (07:36), `example_queue.csv` and `example_queue.audit.json` (07:33), and all six files under `results/figures/`: `gain_curves`, `recall_comparison`, and `topic_recall`, each PNG/SVG (07:28). `run.log`, `runs.csv`, and the 105 JSON files under `results/audits/` are original-run records (07:17–07:25), used as immutable inputs where specified. None of these is evidence of Phase 1 execution. None is modified except the explicitly requested execution-record path rename.
- All Phase 1 generated tables, figures, census, and execution evidence belong under `phase1_outputs/`. The notebook itself will retain real execution counts and captured outputs. Staged clean-run outputs will be retained for inspection; publication to the top-level Phase 1 output directory occurs only after exact repeat agreement.

### Phase 1 repeatability FAILURE — stopped, not approved

- Both fresh subprocesses completed all eight code cells. Each staged `executed_notebook.ipynb` contains execution counts 1–8 and stored outputs. The two executed notebooks are byte-identical. Both runs persisted 2520 run/metric entries and 504 contrast records, preserving censoring and topic-coverage columns, along with reader tables and 15 figure files.
- Exact comparison covered 42 files per run: 40 matched and two differed, `figures/curves_gain.svg` (3781239 vs 4049613 bytes) and `figures/curves_recall.png` (327860 vs 577517 bytes). The run-1 recall PNG could not be decoded: Pillow reported a truncated image. RESOLVED by the user's subsequent diagnosis as a write-completion failure, not analysis nondeterminism: the run-1 SVG ends mid-attribute and the PNG is truncated. This remains a historical FAILED artifact-completeness gate, even though all numerical tables, HTML, and stored notebooks matched. No tolerance, assertion, or expected value was changed at that stop; the subsequent user-authorized repair is documented below.

### 2026-09-07: write-completion repair and user-amended Phase 1 gate

- Atomic figure writer: temporary file in destination directory, save, flush, os.fsync, close, then os.replace. The existing subprocess.run already waits for full exit; retain this ordering and validate completeness before hashing. PNG decoding, SVG XML parsing, JSON/notebook parsing, strict CSV parsing (including gzip CRC), and PDF decoding are mandatory after each run. Failed historical runs are retained unchanged.
- User-amended comparison: nonfigure files (tables, notebooks, JSON/CSV, HTML/Markdown) remain byte-exact; PNGs compare decoded pixels and SVGs parsed element trees. Supplemental PDFs compare decoded pages at 100 dpi. No numerical tolerance is relaxed. Record raw byte hashes separately from decoded-content agreement.
- Presentation decision, made before the new executions: 128 rounded log-spaced review positions plus endpoints and observed 1000/R/2R cutoffs; explicit 100 dpi. This targets figure files under 500 KB while retaining vector axes/labels/legends. It changes display sampling only: all original values, exact target depths, and all 525000 gain positions remain in the tables. Displayed step segments must not be treated as exact intermediate trajectories.
- Add BCa intervals alongside existing percentile intervals and the five raw topic differences, using the identical retained bootstrap distribution. Exact sign-flip remains the primary inferential statement. Before computing BCa, define a material-disagreement diagnostic: different inclusion of zero or either endpoint moving by at least 25% of percentile width. This is an exploratory reporting convention, not a validated threshold. Report undefined BCa explicitly; never substitute a single supposedly reliable interval. Agreement does not establish coverage with five topics.
- Lead all current summaries with fully observed recall@R (0/105 runs and 0/21 pairs censored), retaining original recall@1000. Report the deeper-budget limitation second, including the recall@4R family in the complete census. RESULTS.md will be regenerated from the matching analysis tables only after the amended gate passes.
- DEFERRED Phase 3 exit criterion: after instrumentation passes its replay test, rerun all five topics to budget=collection size (N=23149 in the present subset) with the new independent RNG streams, as a distinct results generation. Preserve the v1 audit history and record new-stream provenance. Do not run experiment.py in Phase 1 or Phase 2. No census learning run is authorized now.

### Amended gate: new write-completion failure after first execution

- Reportable analytical core remains recall@R: 0/105 runs and 0/21 pairs censored. The new first execution completed eight cells and wrote all metric/contrast tables, including BCa and raw five-topic differences, and a staged RESULTS.md leading with the fully observed measures. No learning experiments were run.
- The parent waited for full subprocess exit. The completeness gate then FAILED on `figures/.writing-gobjx86r`, an abandoned temporary file. Subsequent read-only inventory found this file at 128524 bytes and another temporary file `.writing-fq6y155f` at zero bytes, alongside the 15 final figure files. The atomic writer code performs flush/fsync/close/os.replace and finally cleanup; why temporary files remain despite completed worker exit is unresolved. This is a write-completion/persistence failure, not evidence of analytical nondeterminism.
- The second execution was not started. No cross-run agreement is claimed, no assertion or completeness criterion was relaxed, and neither the staged RESULTS.md nor the notebook was promoted as passing. The original instruction in the uploaded study specification says to report a failed test and stop; this stop follows that requirement. All artifacts and the failed receipt are preserved in the new reproducibility session, and the previous execution receipt is retained there separately.
- Presentation target not yet met for three SVGs: gain=676786 bytes, recall=671957, normalized=668022. The remaining 12 figure files are below 500000 bytes. Further rendering reduction is needed; no repair or rerun was attempted after this gate failure.
- New persisted interval sensitivity: 27/112 fully observed metric/pair contrasts meet the disclosed material-disagreement diagnostic (aliases included). These are first-execution observations, not a successful two-run verification. Both intervals and raw differences remain available; exact sign-flip is primary. The complete census includes all recall@4R cutoffs. The regenerated report remains staged until the gate passes.

### Final user-scoped Phase 1 gate and housekeeping correction

- The user diagnosed the two abandoned .writing files as litter: all 15 final figures were present and complete; no output was missing. The previous classification as a write-completion failure is withdrawn. The helper already contained a finally block on inspection; cleanup has been strengthened to cover temporary-file creation and unconditional missing-ok deletion, with an explicit post-exit sweep of .writing-* files. The precise mechanism that left the historical scratch files is not independently established. Removed names and sizes are logged as housekeeping warnings.
- The stop-on-failure rule now applies ONLY to scientific assertions, numerical comparisons, integrity/digest checks, and input validation. Rendering, file size, and artifact housekeeping are warnings, not stop conditions. The 500 KB target is withdrawn entirely. Existing log-spaced presentation is retained; no further rendering optimization is attempted.
- Final PASS requires exact bytes across the two runs for CSV (including compressed gain CSV), JSON, Markdown, HTML, and executed notebooks; every final figure must be complete and decodable; protected-file digests must remain unchanged. Figure byte/pixel identity is NOT required and differing figure names are logged as warnings. Figure size has no threshold. An incomplete figure still prevents claiming PASS until a complete presentation artifact is available, but does not invoke the scientific stop rule.
- Resume the existing completed first execution, remove only its authorized scratch litter, validate its final artifacts, and execute run2 in a fresh subprocess. Do not rerun the learner or alter the first run's numerical outputs. A reader report is rendered independently from each run's persisted tables, then included in exact comparison. Its passing copy becomes root RESULTS.md and phase1_outputs/RESULTS.md; original notebook-generated RESULTS.md remains in each run for provenance.
- The final reader report leads with fully observed recall@R, retains recall@1000, shows every estimable contrast with exact sign-flip, both intervals and five raw topic differences, then includes the complete censoring census (including 4R) and a scoped can/cannot-support paragraph. Phase 2 remains unauthorized.

### Phase 1 final gate PASSED — awaiting user review

- Recall@R remains fully observed (0/105 runs, 0/21 policy pairs censored). AutoTAR mean recall@R is 65.272%; recall@1000 is retained. The final report contains all seven policies, exact tests, percentile and BCa intervals, and five topic differences for each estimable contrast, followed by the complete 24-metric census including the 4R family.
- The existing first execution and the new second execution agree byte-for-byte on all 44 generated files: 29 nonfigure files and 15 figures. Figure identity was observed but was not required. Every final figure decoded successfully in both runs. All 129 protected-file digests match, with only the authorized historical path mapping. No numerical assertion or tolerance was relaxed.
- Both notebooks contain counts 1–8 and 48 stored outputs. Persisted tables contain 2520 metric records and 504 contrasts; 112 contrasts are fully estimable and 392 withheld, including aliases. BCa/percentile materially disagree in 27/112 estimable contrasts under the disclosed diagnostic; neither interval is asserted reliable at five topics.
- Housekeeping warnings record removal of two scratch files from each run. No final figure differs. The source helper's finally cleanup plus explicit post-exit sweep ensures scratch litter is not a scientific failure. No data was lost.
- RESULTS.md, phase1_outputs tables/reports/figures, the stored-output notebook and the final execution receipt have been promoted for review. Earlier failed receipts remain preserved. No experiment.py or collection loader was invoked; no census learning run was performed. Phase 2 has NOT begun and requires user review authorization.

### Phase 1 APPROVED; Phase 1A outcome-informed amendment in progress

- User approved Phase 1 and authorized Phase 2 subject to completing Phase 1A and stopping for review first. Current work is analysis-only. No audits_v1_frozen initialization or learner edits occur before this gate.
- Files touched: analysis/phase1.py (three overshoot metrics); analysis/phase1a.py (archived-input amendment runner and checks); analysis/review_report.py (overshoot-led report and companion); experiment_plan_v2.json (post-outcome additions and Phase 3B approval gate); METRICS.md; docs/prespecification.md; CHANGES.md; generated phase1a_outputs tables/report/verification and root RESULTS.md. Approved Phase 1 outputs/notebook and original source/plan/results remain unchanged. New generated files are listed in the completion entry.
- Overshoot is calculated within run before aggregation. If either component is censored, overshoot is censored with reason component_censored and only the trivial lower bound zero. This zero is never an observed value. No success-only means or interval subtraction. Same exact sign-flip and two bootstrap variants apply; no statistical-significance claim is permitted.
- Correction identified by reading approved tables BEFORE amendment computation: auto_tar-minus-fixed_20 exact effort@75 is +19.6 docs, percentile [-8.0667,51.0667], p=0.3125. The supplied -1.06 docs / [-1.47,-0.65] does not match that contrast or its archived normalized variants. No matching mean near -1.06 was found in the approved contrast CSV. This is a narrative/data-reference discrepancy, not a failed numerical assertion or invalid archived input. Retain the verified value; do not claim growing batches cross one document earlier. The committed difference +97.6 decomposes into +19.6 exact effort and +78.0 overshoot.
- Recall@(R+100) increases review allowance for C16 but does not improve recall's 100/49 pp per-document granularity. The companion and original recall@1000 remain visible. The exploration claim reports four positive nonzero topic differences, a C16 tie, magnitude about 2 pp and exact p=0.125; it is not equivalence or statistical significance. Frozen/similarity ranking reversal is reported without treating nonsignificance as proof of equivalence.
- Carried forward, NOT implemented: Phase 2's exact five-item scope; Phase 3 margins-only replay instrumentation plus wall-clock fit time; row-index versus lexical document-ID tie blocker; Phase 3B combined 12-policy census/schedule run requires fit/time estimates and explicit approval before execution; Phase 4 rates must be supplied by user, plus crossover and schedule frontier. Do not run experiment.py now.

### Phase 1A checks PASSED; STOP for review

- Overshoot contrasts (auto_tar minus fixed_20, docs): 75%=+78.0, percentile [35.4667,131.5333], BCa [39.7333,134.2432]; 80%=+88.7333, percentile [26.6667,175.7333], BCa [33.0,205.2]; 90%=+131.2, percentile and BCa both [91.4,169.8667]. All five topic differences are positive at every target; each exact sign-flip p=0.0625. These findings are NOT described as statistically significant.
- Archived input validation, all 129 protected digests, exact equality of all 2520 preexisting metric CSV records, and component identity/nonnegativity/censoring for all 315 overshoot records PASSED. The expanded tables contain 2835 metric records and 567 contrasts. No external ground-truth fixture values were constructed. No learning runs, figure regeneration, or approved Phase 1 notebook execution was performed.
- Generated files, all in phase1a_outputs/: metrics_by_run.csv, metrics_by_topic.csv, metrics_by_method.csv, paired_statistics.csv, paired_topic_differences.csv, primary_metrics_display.csv, censoring_census.csv, review_results.md, RESULTS.md, verification.json, and approved_phase1_RESULTS.md (preserved prior report). Root RESULTS.md is the new reader report. Existing phase1_outputs remains the approved Phase 1 record; the analysis-only amendment is reproduced with python -m analysis.phase1a.
- Remaining work is deliberately uncompleted at this gate: Phase 2 audit freezing/README, named seed_doc/temp_negatives/exploration/certification streams, epsilon carry with intentional size>=10 gate, strict full-config/model-parameter wiring, and seed_only_frozen rename. Tail/global remains deferred. No learner source has been edited.
- Phase 3 remains uncompleted: round/fit IDs, temporary-negative IDs, selection operators, candidate margins or declared truncation, fit parameters, batch growth state and wall-clock fit time; audit-only row_order replay must pass. Row-index versus string-ID tie ordering remains a Phase 4 blocker.
- Phase 3B remains uncompleted and requires its own estimate/approval gate: combined 5-topic x 3-seed x 12-policy census grid at 23149, including seven existing policies and fixed_10/fixed_50/fixed_100/grow_5pct/grow_20pct. Report total fits and wall-clock estimates by policy before running; propose but do not impose caps if needed. No separate census generation now.
- Phase 4 remains uncompleted: v1/v2 comparison, review/QC/compute rates supplied by user, committed/compute/total/overshoot costs, crossover and committed-effort-versus-fits frontier. Do not invent rates. Phase 1A is ready for review; no later phase begins in this turn.
- `phase1_outputs/notebook_execution.json` records FAILED with the exact execution method, library versions, fresh-process/namespace declarations, prior network failure, and comparison scope. Both runs and both SHA-256 inventories are retained beneath the receipt's `reproducibility_directory`. Nothing was promoted as a passing final notebook/report. Each run's `verification.json` concerns archived-input consistency only and does not supersede this failed cross-run gate.
- The renamed v1 execution record retains its original bytes. Protected inputs passed their original digest checks before and after both worker executions, applying only the authorized path mapping. Phases 2–5 remain untouched.

### Censoring finding from the persisted, matching tables

Each metric has 105 runs and 21 full-topic policy comparisons. `censoring_census.csv` in each run lists every metric, including aliases, with exact run and pair topic counts. One censored seed in either policy on any topic withholds that pair's full-topic inference; topic-level pair counts overlap.

| Metric family | Censored runs /105 | Censored pairs /21 | Topics driving run censoring |
| --- | ---: | ---: | --- |
| recall@1000, recall@R, recall@(R+0), recall@(R+100) | 0 | 0 | None |
| recall@(R+1000), recall@2R, recall@(2R+0/100/1000) | 21 | 21 | C15:21 |
| recall@(4R+0/100) | 42 | 21 | C15:21; C18:21 |
| recall@(4R+1000) | 63 | 21 | C15:21; C18:21; GCRIM:21 |
| Each 75% target metric | 43 | 18 | C12:6; C15:12; C16:7; C18:9; GCRIM:9 |
| Each 80% target metric | 46 | 18 | C12:7; C15:12; C16:8; C18:9; GCRIM:10 |
| Each 90% target metric | 58 | 20 | C12:12; C15:12; C16:10; C18:12; GCRIM:12 |

Each target family includes exact effort, whole-batch effort, depth/N, and depth/R. Most deeper-target comparisons are censored: 18/21 at 75% and 80%, and 20/21 at 90%. The 5000-document budget is too short to compare the complete seven-policy set at these targets across all five topics. This is a limitation of the study design, not evidence of equivalence or grounds for a success-only average. Across all 24 metrics, 392/504 contrasts are withheld and 112 have full topic coverage; those counts include metric aliases and are not independent experiments. Recall@2R's four-topic partial means explicitly exclude C15 and cannot stand in for the five-topic comparison.


### Phase 2 implementation — verified Git baseline, no experimental reruns

- Clone baseline: main was clean and up to date with origin/main. git log --oneline reported f6045c6 "v1 baseline: original study plus Phase 1 and 1A analysis". v1-frozen resolves to f6045c63a4bcd1c8f2e6c5735dd4da120b706233. All 124 committed results/ files, including all 105 audits, matched the tag. All 129 original manifest digests passed before edits. AGENTS.md and this log were read before work.
- User-approved migration replaces audits_v1_frozen with the Git tag. No duplicate audit directory was created. Do not move/rewrite v1-frozen. Keep the original SHA-256 manifest through Phase 2 as a cross-check: compare its original source digests against the tag, and all other protected files against the worktree. Proposed retirement only after user review: Git tag/tree comparisons plus explicit data-source checksum validation can replace the hand-maintained 129-file manifest; no retirement occurs now.
- Restored 180 omitted evidence files from the retained workspace: notebooks/01_results.ipynb, phase1_outputs/notebook_execution.json, and the entire 178-file reproducibility tree. The smaller Phase 1A migration bundle omitted them; they were not pruned. Historical failed attempts and the final passing two-run evidence are preserved byte-for-byte. Earlier FAILED wording in this log is historical and superseded by the final PASSED receipt.
- RESULTS.md wording only: exact-effort differences are unresolved, not equal. Overshoot contributes 78.0/97.6=79.9% of the 75% committed-effort gap. The exact-effort row, mixed topic signs and both intervals remain visible. No earlier/later crossing or significance claim is made. No analysis module, metric or archived table was changed.
- RNG: src/learner.py derives seed_doc, temp_negatives, exploration, certification independently using SHA-256 of the versioned namespace, integer seed and stable stream name (not Python hash()). src/experiment.py uses the learner's seed_doc stream for the common positive seed. Random-policy permutations use exploration; certification is reserved and never consumed. SVM random_state remains the run seed. auto_tar's temporary-negative count, replacement policy, ranking/ties, retraining and growth rule remain unchanged. New results are NOT comparable to v1-frozen as exact paired trajectories because realized draws change.
- Exploration: configurable epsilon defaults to 0.1. A rational carry replaces per-batch truncation/minimum-one; cumulative assigned exploration differs from epsilon times eligible review slots by less than one. Eligible means size>=10 after limit/pool clipping; seed and gated batches are excluded. This denominator is necessary: retaining the mandatory gate makes a global bound over ALL early reviews impossible. Gate behavior and exploit-first tail sampling remain unchanged. There is no new tail/global operator.
- Config: src/config.py validates explicit top-level/nested keys and all supplied LinearSVC parameter names, resolves original SVM defaults and epsilon, and reserves random_state for the run seed. The full resolved plan is passed through ReviewLearner.fit into new_svm. Metadata-only snapshots record the resolved config and actual get_params() for each seed in phase2_outputs, never results/. Future experiment tasks carry the same plan/output directory. The execution flag stays false; no collection loader, simulate(), experiment main(), model training or new experiment was run.
- Rename: frozen_svm is now seed_only_frozen in the learner, future policy config and legacy report identifiers; fit-once behavior is unchanged. Historical plans, results, reports and audits retain their old names. The v1 tag is the historical lookup. Legacy reporting code was not executed or otherwise modernized; Phase 4 analysis remains deferred.
- Files changed: RESULTS.md, experiment_plan_v2.json, src/learner.py, src/experiment.py, src/summarize.py (policy names only), docs/prespecification.md and CHANGES.md. Files added: src/config.py, tests/test_phase2.py, phase2_outputs/resolved_config.json, model_parameters.json, unit_tests.txt and integrity.json, plus restored evidence paths above. No original results/ file or experiment_plan.json changed.
- Seven targeted regression/contract tests PASSED, including deliberate C=2.5 changing the instantiated model via the learner fit-construction path, isolated/reproducible streams, epsilon/gate/carry contracts, unknown-key rejection, renamed frozen versus continuously retrained behavior, and actual parameter snapshots with frozen-output-path rejection. Fit calls were mocked; no model training or hand-computed retrieval fixtures were used. No assertion, expected value or tolerance was changed to pass a test.
- Remaining work: Phase 3 instrumentation/replay (including wall-clock fit time) is NOT authorized. Phase 3B combined census/schedule run still requires compute estimates and explicit approval; no run was launched. Phase 4 rates/crossover/frontier and TARexp tie-order blocker remain deferred. The original manifest remains retained pending the proposed retirement review. Stop after the Phase 2 gate commit.


### Phase 2 pre-push packaging correction

- The restored evidence had re-added 20 SVGs and four gain_curves.csv.gz files under phase1_outputs/reproducibility, which the user intentionally excluded as regenerable duplicates. Removed these 24 files from the unpushed commit/index, retained local copies, and added the two exact user-supplied ignore patterns. Their committed hash inventories and source audits remain unchanged. This supersedes the earlier implication that every restored file should be tracked.
- Amending the unpushed Phase 2 commit changes its SHA; no published history or v1-frozen tag is rewritten. No analysis, scientific assertions, source code or test expectations changed.

### Phase 3 instrumentation — 2026-09-07

- Synced from clean phase2-complete (f0619a844366d08b10c4191b105342a9b36fe512), on branch phase3-instrumentation. Read AGENTS.md and this history. Phase 3 alone is authorized; historical entries saying it was unauthorized are superseded by the user's current instruction.
- Added schema-3 seed/query round events, fit indices, current-round temporary-negative external IDs, per-document operators, full raw candidate margins, actual fit-time model parameters, scheduled batch sizes before/after growth, and perf_counter fit seconds. Frozen rounds reference their original fit; no duplicate fit cost or stale negative draw is reported. Timing excludes slicing/ranking/serialization. No probabilities, new sampling operator, changed ranking/tie rule, RNG draws or model-fitting semantics.
- Random-policy stream naming is intentionally retained: random and explore_10 are mutually exclusive policies, so random is the sole consumer of exploration in its run. Sharing a name therefore introduces no coupling. Renaming would change random trajectories without benefiting this instrumentation phase.
- src/audit.py reconstructs complete row_order from round events alone; the future writer checks exact agreement. Ten tests passed (seven carried-forward, three new), including all seven policies with mocked fits and JSON round-trip replay without a model or flat row_order. No expected value, tolerance or scientific assertion was changed. No retrieval ground-truth fixtures were computed.
- Storage is estimated structurally, not measured from new experiments. Full fixed-20 margins cost approximately 125–250 MB/run at 5,000 and 328–655 MB/run at census; growing SVM arms approximately 26–51 and 31–63 MB. Detailed assumptions and all policies are recorded in docs/audit_schema.md and phase3_outputs/audit_size_estimates.json. Proposed top-1,000 plus selected margins is NOT implemented or enabled; full remains the only accepted scope pending review. Serialized estimates do not bound Python RAM.
- Files touched: src/learner.py, src/config.py, src/experiment.py, CHANGES.md. Files added: src/audit.py, tests/test_phase3.py, docs/audit_schema.md, phase3_outputs/estimate_audit_size.py, audit_size_estimates.json, unit_tests.txt and integrity.json. All results/ files and experiment_plan.json match phase2-complete by SHA-256. No historical outputs, plans, notebooks, figures or analysis were regenerated. Excluded reproducibility SVG/gain archives remain excluded. The retained historical manifest is not retired without review.
- Gate: instrumentation tests and protected-file checks PASS. No experiment.py execution, collection loading or actual model fitting took place. Remaining uncompleted work is explicitly deferred: real-run audit size measurement, full-collection runtime estimation/runs, any approved truncation, Phase 4 costs, probability-based stopping and TARexp matching. Numeric-row versus string-ID ties remain the known blocker. Stop at Phase 3; Phase 3B is not authorized. GitHub integration was previously confirmed read-only; supply a patch for local push rather than claim a remote update.

### Phase 3B preflight and approved margin scope — 2026-09-07

- User approved Phase 3 and authorized only margin-scope implementation, storage proposal and census/sweep estimates at this gate. Remote phase3-complete resolves to 86fbf4d5c7230dde21083253121aa841879906a2, not the stated local 1c0c4dd; their file trees match exactly. Started from the clean remote tag. Prior history records ten passing tests, immutable original evidence and outstanding storage/runtime questions.
- Added explicit top_1000_plus_selected scope alongside full; selected in experiment_plan_v2.json only. Every round records the scope and true candidate count; seed round count is N−1 after seed acquisition. Truncated margins retain the ranking's top 1,000 union all selected rows, including exploration and batches above 1,000. No selection, fit, RNG or tie semantics changed. Eleven tests pass, including full-versus-truncated selection identity and raw-margin alignment for relevance, uncertainty and exploration at small and >1,000 batches. No assertions or fixture expectations changed.
- User's storage rationale: prior labels, temporary negatives and actual parameters allow refitting from the feature matrix rather than storing all margins. Qualification: identical training ordering, features, software/RNG and numerical behavior are needed; exact score regeneration is not yet verified. Full-pool distribution analysis is lost from the stored record and requires recomputation. The claim that all batches are below 1,000 is incorrect for census (max actual growing batches 1,096/2,070/3,373 at 5/10/20%); all-selected retention handles this. Truncated scores cannot independently certify that omitted documents scored below the cutoff, even for small batches. These are documentation corrections, not failed assertions or relaxed integrity checks.
- Proposed audit_store/<generation_id>/ ignored directory plus committed audit_manifests/<generation_id>.json with SHA-256, bytes, run identifiers, provenance, expected grid and external archive reference. Full verification/backup procedure is in docs/audit_storage_proposal.md. Await approval; no directory routing, ignore rule or audit generation activated. Gitignored storage alone would not provide durable evidence.
- Full requested grid: 67,395 fits; historical-scaling scenarios 1.64–12.81 worker-hours, ideal two-worker 0.82–6.40 hours plus unmeasured new I/O/startup; 2.19–4.37 GB uncompressed audits. These are projections, not measured runtimes or safe bounds. Known worst-worker memory components total approximately 482 MB; two workers plus parent matrix approximately 985 MB before unknown loader/interpreter/solver allocations. True peak RSS remains unmeasured. No missing fit times or RSS were invented. Estimator reads committed old timing/data metadata only, never the loader.
- Recommendation pending approval: protect original seven and both growth arms, cap fixed_10 first, then fixed_100, then fixed_50. First cap proposed at 5,000 only for fixed_10; documents explicitly warn of censored frontier comparisons. No cap/drop has been made. New schedule arms remain an arithmetic specification until approved implementation/testing before the run.
- Files touched: src/config.py, src/learner.py, src/audit.py, src/experiment.py, experiment_plan_v2.json, docs/audit_schema.md, CHANGES.md. Added: tests/test_margin_scope.py, docs/audit_storage_proposal.md, docs/phase3b_preflight.md, phase3b_preflight/estimate.py, estimates.json, unit_tests.txt, integrity.json. No results/ or original experiment_plan.json changes. No experiment.py run, loader invocation or actual model fit. Protected-file check recorded separately. Known TARexp numeric-row/string-ID tie blocker and deferred probability arm unchanged. Stop for storage/compute approval; neither experiments nor Phase 4 are authorized.

### Approved full census launch preparation

- User approved all 180 runs, no caps, and the audit-store/manifest scheme. Fixed-10 remains at census as requested. Synced clean origin/main e42d733 (same tree as local preflight commit). Read AGENTS.md and prior corrections. New estimate widens the ranking window to max(1000, scheduled batch before growth + 100), clipped to the candidate pool and union all selected rows: 2.189–4.379 GB total, approximately 0.12% above prior estimate, not material. Actual selected count may be budget-clipped; using scheduled size preserves at least the requested band. Full and prior scope remain available; every round records rule/requested/resolved window and candidate count. No probability conversion or changed auto_tar trajectory semantics.
- Transfer settled before data generation: 60 independent policy/topic ZIPs, three seeds each; approximately 6–385 MB uncompressed content per piece before compression. Downloadable files, transfer index/checksums and a verification script are to be delivered; no claim of durable archival until uploads succeed. Audit files are ignored at audit_store/<generation_id>; no audits go into git. Ignored raw tokens are copied from the retained original workspace only after SHA-256 matches committed source provenance. Source lock is retained for future verification.
- Added the five approved schedule arms, retaining the exact 10%-growth expression for original arms; fixed schedules preserve their named batch size. Fourteen contract tests pass, including complete audit-only replay across all 12 policies, widened-margin/selection identity and schedule recurrence tests; actual fits mocked. No retrieval fixture values or assertions were altered. Protected original results/ and experiment_plan.json digests pass.
- New runner handles the approved grid, independent atomic audit writes, per-run parsed replay/census validation, incremental manifest, and fail-fast worker termination on integrity/scientific errors. Solver convergence warnings are treated as scientific failures rather than silently accepted. The execution-only configs/census.json authorizes this generation; historical plans remain unchanged. Resource monitoring records total elapsed time, sampled process-tree RSS and per-process high-water values; earlier under-seven-hour and under-one-GB figures are projections, not guaranteed bounds. Environment has approximately 14 GiB memory limit, eight CPU quota equivalents and 30 GB free disk. Two workers remain the planned concurrency.
- New/changed files for this preparation: .gitignore, src/config.py, src/learner.py, src/audit.py, src/experiment.py; added src/census_io.py, src/census.py, src/monitor_census.py, tests/test_census_contracts.py, configs/census.json, data/source_lock.json and phase3b_outputs/{estimate_storage.py,estimates.json,unit_tests.txt,generation.json,pre_run_integrity.json}; updated docs/audit_storage_proposal.md and CHANGES.md. Census artifacts/manifests/transfer tooling will be logged at completion or a failure stop. No Phase 4 analysis is authorized.

### Census running — resource observation correction

- Launch code commit is 6a83f0e; manifest records that exact training/audit implementation. No fit or selection code has been changed during the run. Initial complete audits pass parsed JSON, full-order replay, candidate/margin alignment, and census coverage checks.
- Inspection found the original resource wrapper used namespace-local subprocess IDs against a host-mounted /proc. Its aggregate RSS/per-process map is therefore unusable, even if it reports a number. This is a monitoring implementation defect found by inspection, not a failed scientific comparison. Worker resource.getrusage(RUSAGE_SELF).ru_maxrss remains valid. A separate host-PID-aware sidecar now samples the existing run without restarting, signaling or changing it; its per-process VmHWM includes earlier lifetime peaks, while its aggregate samples begin after launch. src/monitor_census.py is corrected for future invocations only. Original resource receipt is preserved and explicitly superseded for RSS, not for elapsed time/exit code. ps also emitted an environment library error; direct /proc inspection was used instead. Neither issue altered experimental data or assertions.
- Added scripts/observe_census_rss.py for this observation, src/package_audits.py and scripts/verify_audits.py for the agreed independent ZIP delivery and standard-library verification. These are packaging/monitoring changes; actual run code stays at the recorded launch commit. All delivery and final completeness claims remain pending.

### Census FAILED — stopped and evidence preserved

- Scientific stop: C15 / seed 11 / uncertainty raised sklearn.exceptions.ConvergenceWarning: Liblinear failed to converge at the unchanged max_iter=10000. The predeclared launch warning policy promoted this to an error. Remaining workers were terminated. No max_iter, tolerance, expected value, assertion, warning policy or model parameter was relaxed; no failed/canceled run was retried. Phase 4 was not started.
- 38/180 runs completed and passed audit validation; 11,165 successful fits are recorded in those completed runs (not a count of all attempted fits). One run failed and 141 other grid entries lack a completed audit. Total completed audit bytes: 464,693,139 versus estimated 381,060,248–762,120,496 for precisely that subset. All 38 declared-complete files are parseable/full-census and their SHA-256 digests are preserved. This is NOT a complete or passing generation.
- Wall time to stop including wrapper: 858.805 seconds (14.31 minutes); no full-grid duration was measured. Valid host-aware RSS: sampled process-tree peak 1,174,904 KiB (~1.12 GiB), maximum worker high-water 407,444 KiB (~398 MiB), parent high-water 581,520 KiB. Observer began after launch; lifetime per-process high-water includes earlier peaks, aggregate sampling does not. Original wrapper's zero RSS is invalid due to namespace mismatch and is retained as such. Runtime/memory estimates were not certified bounds.
- Two incomplete .writing scratch files (9,961,989 and 24,186,887 bytes) were present at the failure stop. Their JSON is truncated, so they are NOT promoted or included in the 38 completed runs. Bytes/digests are preserved as diagnostics. Their precise run attribution is not established. The failed uncertainty fit's round index and temporary-negative draw were not persisted because audits are written at successful run end; recovering those would require a separately authorized diagnostic reproduction. Do not imply complete failure-round instrumentation.
- The full-generation packager and verifier were NOT relaxed to accept failure. A separate preservation-only packaging step saved the completed subset as 15 explicitly FAILED policy/topic ZIPs, plus one diagnostics ZIP with the two incomplete writes. All 16 files were successfully saved for download (completed-audit ZIPs total 179,519,843 bytes; diagnostics ZIP 13,576,553 bytes). Each completed-audit ZIP has per-file SHA-256 instructions and a failed-manifest snapshot; snapshots predate final delivery metadata but preserve identical audit digests. The final committed manifest records archive bytes/checksums/download identifiers and explicitly retains FAILED status.
- Gate artifacts added: audit_manifests/20260907_census_18add1f5ba0e.json and phase3b_outputs/20260907_census_18add1f5ba0e/{resolved_config.json,model_parameters.json,runs.csv,run.log,resources.json,resources_host_pid.json,failure_report.json,transfer_index.json,delivery_receipt.json}. ZIPs and temporary transfer request/receipt files remain ignored under transfer_packages; audits/scratch writes remain ignored under audit_store. No raw audit or archive is committed. A separate downloadable index lists all 16 preserved packages.
- Final protected-file integrity: 125 files (124 results/ files and experiment_plan.json) match origin baseline. No historical analysis, outcomes, notebook or primary metric was changed. Fourteen pre-run contract tests passed, but the scientific run gate FAILED. All uncompleted grid work, diagnosis/recovery and Phase 4 remain stopped pending user direction. Commit this failure record and provide a patch; do not label Phase 3B complete.

### Prior-question gate: v1 convergence audit completed; nonzero — STOP

- User authorized retrospective refitting of the archived v1 trajectories before any solver-limit investigation or census rerun. Checked the byte-identical v1-frozen learner, original separate seed selection and original shared learner RNG (including exploration draw consumption), original labeled prefixes, original hard-coded model parameters and matching Python/NumPy/SciPy/scikit-learn versions/source-data digest. No current-stream learner was substituted. Temporary-negative IDs are reconstructed from frozen deterministic code; v1 did not log them directly. Every reconstructed batch and fit count matched the archives exactly across all 105 runs.
- All 5,880 fits were inspected; eight emitted ConvergenceWarning and reported n_iter_=10000. These occur in six C15 runs: fixed_20 four fits (seed 11 fit 209; seed 29 fit 250; seed 47 fits 221 and 247), uncertainty three (seed 11 fit 9; seed 29 fits 40 and 47), auto_tar one (seed 47 fit 47). Other topics and SVM policies had zero warnings. Random and seed_similarity have no model fits. The diagnostic completed in 716.989 seconds; this is not a new outcomes generation or a solver-limit search.
- Correction to an interim chat update: the statement that C15/11/uncertainty had no warning was wrong; it was inferred from a log tail displaying other policies. Its per-fit ledger records a warning at fit 9, after 37 reviewed documents. This was explicitly corrected during the check. Final counts come from the complete ledgers, not progress-log inference.
- Relation to the overshoot finding was read from existing phase1_outputs/metrics_by_run.csv, without recomputing outcomes or fixtures. C15/11/fixed_20 fit 209 occurs after 4,161 reviews, before the recorded 90% crossing at 4,283 and committed boundary 4,301. That 90% trajectory contains an upstream non-converged fit. Other flagged auto_tar/fixed_20 fits are after their corresponding 90% measurements. No flagged fit in those two arms precedes their 75% or 80% targets. Effect on a fully converged counterfactual remains unknown; no claim of unchanged results or a reversed finding is justified.
- The diagnostic records warnings and iteration counts as the requested observations and completes the v1 inventory; it does not suppress or relax a scientific assertion. No max_iter, C, loss, dual, tol, assertion, tolerance, historical result, or plan was changed. Both plans and all original results pass protected-file checks. Per-fit ledgers include actual parameters, reconstructed temporary-negative rows/IDs and the archived labeled-prefix reference; kept outside git under audit_store/v1_convergence_check and included in the downloadable evidence archive.
- Files added: diagnostics/v1_convergence/{check.py,source/learner_v1.py,check.log,report.json,summarize.py,findings.json,nonconverged_fits.csv,README.md,integrity.json}; CHANGES.md appended. No experimental/analysis implementation was changed. STOP at the user's nonzero-v1 gate. Empirical max_iter search, permanent convergence instrumentation changes, new numerical budget, full-grid rerun and Phase 4 are unstarted. docs/PAPER_COMPARISON.md amendment is deferred: convergence trouble already existed at 5,000, and no larger successful iteration budget has been established. Do not write the proposed census-only causal claim. The earlier generation remains FAILED; without a replacement generation it is not yet marked superseded by one.

### v1 original-log corroboration

- Read-only search of original results/run.log found eight contemporaneous ConvergenceWarning messages at lines 22, 24, 33, 40, 44, 46, 55 and 57. Thus the assertion that v1 convergence was entirely unlogged/silent is incorrect: warnings were already present and were missed in earlier review, but had no per-fit identity or iteration count. The original warning count corroborates the eight retrospectively flagged fits. Do not attribute individual parallel log lines to fits by adjacency; the deterministic reconstruction supplies attribution. Added original warning lines/log SHA-256 to diagnostics/v1_convergence/findings.json and this clarification to its README. No new fits or later-phase work followed the nonzero gate.

### Authorized fit-209 sensitivity check — corrections before fitting

- Read AGENTS.md and the convergence-audit history. The user retracts the proposed census-depth framing: all eight v1 non-converged fits occurred within the original 5,000-review budget, on C15 (R=4,179). Concentration on the highest-prevalence topic motivates a prevalence/training-composition hypothesis; it does not establish a causal explanation. No depth claim is added to docs/PAPER_COMPARISON.md.
- RESULTS.md now prominently discloses all eight failures and the upstream C15/11/fixed_20 fit 209 in the only uncensored batch_effort_at_90 contrast. It states that the +193.9-document headline comparison has a known solver defect in one of its five topic values, with sensitivity unknown and unresolved as of v1. Historical values remain unchanged. This disclosure precedes any new diagnostic fit.
- AGENTS.md adds mandatory inspection of run logs and captured warnings at every phase gate; passing tests alone are insufficient. Earlier gates missed all eight original warnings. This is a process correction, not a claim that tests or archived values changed.
- Authorized scope now: isolated sensitivity of C15/11/fixed_20 fit 209, with identical training rows/order/labels, reconstructed temporary negatives, seed and model parameters except max_iter. Use 100,000 as a deliberately roomy diagnostic ceiling (10 times the original), not an empirically established requirement. Report n_iter_ actually used and warning status. If that ceiling is reached, report and stop without raising it again. Full candidate-set Spearman correlation uses average ranks for ties; selection retains numeric-row tie-breaking. Compare set membership and ordered batch identity separately. Replay the remainder under the original per-fit max_iter=10,000 after substituting only fit 209, even if its selected batch matches, to test the isolated intervention directly. This is not a fully reoptimized run or the full-grid rerun. Both plans and all production solver settings remain unchanged until separately approved.

### Fit-209 sensitivity result — isolated concern closed; rerun gate remains

- Original C15/11/fixed_20 fit 209 reproduced its n_iter_=10000, ConvergenceWarning and exact archived selected batch. With only max_iter increased to the predeclared 100,000 diagnostic ceiling, the solver stopped at n_iter_=10190 without warning. The measured requirement for this fit is its observed 10,190 iterations, not the 100,000 ceiling; no global limit has been chosen. Full-candidate Spearman correlation (18,988 candidates; tie-aware ranks) is 0.999999929314762. Selected set replacements: zero; symmetric difference: zero; ordered batch positions changed: zero.
- Although the batch matched, all 41 subsequent fits were replayed under the original max_iter=10000 to measure the downstream effect explicitly. The original RNG state was reconstructed through all 209 negative draws, each checked against the prior diagnostic ledger. No downstream warnings occurred. Complete row_order is exactly identical across all 5,000 positions. Effort_at_90=4283 and batch_effort_at_90=4301 remain identical to the persisted historical values. The isolated change to the five-topic/three-seed +193.9 committed-effort contrast is zero documents. This closes the concern about completing fit 209 alone; no claim is made that the other capped fits or an all-fits reoptimization are harmless. No nonsignificance-to-equivalence inference is involved.
- RESULTS.md keeps the requested prominent historical v1 disclosure and adds a separately labeled subsequent diagnostic with these measured results. User's depth-framing retraction is retained; prevalence/training composition remains a hypothesis rather than an established cause. No census-only claim is added to docs/PAPER_COMPARISON.md.
- New log-inspection gate applied: read results/run.log (eight historical warnings) and the full diagnostics/fit209_sensitivity/check.log plus captured probe/downstream warning arrays. The known baseline warning is an expected control observation, not hidden; the enlarged diagnostic fit and all downstream fits emitted no warnings. gate_receipt.json records log hashes, source-data artifact hashes and unchanged protected files. Both plans and all original results remain unchanged. No assertion/tolerance/expected value was changed to pass a check.
- Files touched: AGENTS.md, RESULTS.md, CHANGES.md. Added diagnostics/fit209_sensitivity/{check.py,check.log,report.json,README.md,gate_receipt.json}. Raw candidate margins and diagnostic replay are preserved outside git in audit_store/fit209_sensitivity and included in a downloadable evidence ZIP. No production learner/config/run implementation changed. No full-grid rerun, empirical census-wide limit search, permanent convergence-logging change, supersession by a replacement generation, or Phase 4 work started. Stop at the requested approval gate.


### Iteration-requirement preflight — integrity failure, STOP (2026-09-08)

- User authorized nine high-ceiling diagnostic refits followed by a fresh full census. Prepared diagnostics/iteration_requirements/probe.py with diagnostic ceiling 1,000,000 and proposed production headroom rule twice the observed maximum rounded upward to the next thousand. Neither is an empirically established production requirement. Input verification stopped the script before any diagnostic fit; no iteration counts were obtained and no census run began.
- Three retrospective v1 convergence ledgers fail their committed report digests: C12/11/auto_tar has 41 records and 86,527 bytes versus 47 fits and 99,218 bytes recorded; C12/47/auto_tar has 42 records and 88,672 bytes versus 47 and 99,223; C16/47/auto_tar has 40 records and 84,457 bytes versus 47 and 99,232. All remaining 102 ledger digests match. These files parse as JSONL but lack the recorded number of fit records. The retained v1_convergence_evidence.zip contains the identical deficient bytes for all three. Cause and time of the loss are unresolved; the archive cannot restore the expected versions. This qualifies the earlier claim that all per-fit evidence was preserved, not the separate immutable original trajectories. Do not replace expected digests with current values.
- Preserved failure report, full probe log, and expected/actual digest, size, record-count and archive comparison in diagnostics/iteration_requirements/{report.json,probe.log,integrity_failure.json}. No failed assertion, tolerance, fixture, input ledger or reference digest was modified. Read all four logs listed in integrity_failure.json: the new probe has an integrity traceback; the historical run has eight ConvergenceWarning messages; fit-209 records the expected baseline warning and no enlarged-fit warning. Absence of warning text in the retrospective progress log does not override its per-fit warning records.
- Files touched: CHANGES.md; added the four diagnostic files above including probe.py. Both plans, production code and results/ remain unchanged; results/ and experiment_plan.json have no diff against v1-frozen. No new production max_iter, permanent convergence instrumentation, empirical nine-fit results, fresh census, completed-run manifest or transfer package was produced. The prior 38-run generation remains FAILED and is not reused. Phase 4 remains unstarted. Stop under AGENTS.md integrity policy; repair/reconstruction requires resolving this gate before retrying.


### Authorized ledger reconstruction — post-exit integrity failure (2026-09-08)

- Simulated the frozen growing schedule before fitting: 47 fitted query rounds plus one seed review; final clipped batch 302. All 15 original auto_tar audits have identical matching batch boundaries and all 15 runs.csv entries report 47 fits. Audit JSON has no dedicated fit_count field. The 47-record expectation stands; no reference value/digest was changed.
- Before reconstruction, RESULTS.md corrected the unsupported denominator: eight known failures among 5,862 retained records, 18 unknown. Reconstructed only the three authorized trajectories, under frozen v1 code, original RNG, original parameters and verified environment/source. Used a separate ignored directory; original ledgers were not overwritten. Captured all warnings and compared every selected batch against the original archive. The process recorded 141 fits, no warnings, and all three closed-file SHA-256 matches against the existing committed expectations.
- Critical correction to interim success report: independent post-exit reads found reconstructed C12/11/auto_tar contains only 41 records / 86,527 bytes and the same deficient digest as its original, despite the producer logging 47 / 99,218 and the matching expected digest. C12/47 and C16/47 retain 47 records and match their expected digests. Preserved producer report/log and separate post_exit_integrity.json without rewriting either receipt. This is a recurring persistence inconsistency, not evidence that the schedule expects fewer fits. Its cause is unresolved. Do not infer model nondeterminism from contradictory on-disk persistence receipts or declare recovery complete.
- Original ZIP entries are exact prefixes of the corresponding observed reconstructed files; C12/11 is currently equal to its deficient archive entry. Missing suffixes originally comprise indices 42–47, 43–47 and 41–47. Writer inspection shows per-record flush and close-before-hash, but no fsync or atomic final publication. This is a durability weakness, not proof of the mechanism or why exactly these three files were affected. The 102 other original ledger digests matched the prior inventory. No broader safety claim is warranted; original results/ and experiment_plan.json still have no diff against v1-frozen.
- Twelve missing records are now recovered with matching digests, all converged. RESULTS.md retains the initial provisional disclosure and adds current evidence: 5,874 retained fit records, eight known failures, six statuses unsupported by retained per-fit evidence. Process-reported convergence for the remaining six is distinguished from preserved evidence. Topic/policy alone cannot establish whether an unexamined fit converged; low prevalence is not a convergence guarantee.
- Files touched: RESULTS.md, CHANGES.md. Added diagnostics/ledger_reconstruction/{reconstruct.py,schedule.json,report.json,reconstruct.log,gate_receipt.json,post_exit_integrity.json}. Full reconstruction log and every captured warning array inspected: no fit warnings; the post-exit integrity failure overrides producer COMPLETE status. No assertion, tolerance, expected digest, solver parameter, plan or original results changed. No max_iter probes, census rerun or Phase 4. Stop at failed reconstruction gate; do not silently retry or replace expected hashes.


### Phase 3C — durable write path and final ledger recovery (2026-09-08)

- Read AGENTS.md and prior failures. Current historical state: two ledgers recovered; C12/11 producer success contradicted by a 41-record final file. User authorizes write-path repair and one reconstruction only; solver-limit probes, census and Phase 4 remain blocked.
- Consolidated every active project-owned artifact writer behind src/durable_io.py: same-directory temporary serialization, flush, file fsync, close, pre-publication parse/count/reference checks, atomic replace, supported directory fsync, final-path readback/parse/count/hash/value verification and repeated final hash. Expected digests never change. A pre-publication failure retains an existing complete destination; detected post-publication corruption removes the invalid destination and raises. Hard process death can leave unpublished scratch. No claim is made about hardware or filesystems that violate fsync/rename contracts.
- Full writer inventory, validation contracts, format qualifications and exceptions in docs/DURABLE_WRITES.md. Census JSON already had file fsync/replace; this phase adds stronger verification and removes the separate ledger path. Workers return final-path audit digests and parents must agree. JSON value checks bind nested records to intended input rather than trusting a digest of a truncated serialization. This increases read/parse cost and transient RAM; no census-scale benchmark or new runtime claim was made.
- Migrated current notebook source only; retained all historical outputs, explicitly labeled as historical rather than execution of changed source. Preserved results/, both plans, all prior diagnostic outputs/ledgers, frozen learner extraction and historical execution copies. Legacy analysis commands still target historical destinations and are not authorized for execution. Third-party caches and Git/editor operations are outside the application writer; external shell redirects must be replaced by the new capture wrapper for future recorded runs.
- Twenty tests pass (14 existing plus six fault/roundtrip contracts). Interrupted/unflushed writes and valid truncated JSONL are rejected without partial final publication. Captured warning/error injection is expected test input, not a hidden scientific failure. No existing assertion/tolerance/expected value was altered. A notebook-edit attempt in the default interpreter lacked nbformat and failed before publication; using the established environment resolved the dependency, with no validation relaxation.
- One C12/11/auto_tar reconstruction: 47 fits/records, 99,218 bytes, original committed SHA-256 038cb83c785ae7a2abe366668eae61925f22b615389763db693344369ba1a23c matched by both producer and independent post-exit process. Zero warnings, max n_iter 2,920. Explicit final inventory verifies all 105 original/recovered ledgers against unchanged committed expectations: 5,880 supported records, eight known non-converged fits, no unknowns. RESULTS.md kept 5,874/eight/six until this verification, then added the resolved count while retaining the provisional history. No original deficient ledger was overwritten.
- Repaired publication retained the correct ledger; missing-record concern is operationally closed. Why exactly three earlier tails were lost remains unproven; successful repaired publication does not identify a unique historical cause. Prior receipt contradictions and missing prefixes are preserved. No generator loss observed with the new path.
- Gate logs fully inspected: both test logs, reconstruction log, captured warning fields in all verified ledgers, and original results/run.log. Eight historical C15 warnings remain recorded; no new convergence warnings. Protected-file bytes match the pre-phase commit. No max_iter probes, numerical-budget changes, census run, convergence-logging implementation or Phase 4. Those remain explicitly uncompleted and blocked. Commit at Phase 3C gate and supply patch plus verified reconstructed-ledger package.
- Files touched/added in this phase:
  - AGENTS.md
  - CHANGES.md
  - RESULTS.md
  - analysis/artifact_io.py
  - analysis/execute_notebook.py
  - analysis/phase1a.py
  - diagnostics/durable_reconstruction/post_exit_verification.json
  - diagnostics/durable_reconstruction/reconstruct.log
  - diagnostics/durable_reconstruction/reconstruct.py
  - diagnostics/durable_reconstruction/report.json
  - diagnostics/fit209_sensitivity/check.py
  - diagnostics/iteration_requirements/probe.py
  - diagnostics/ledger_reconstruction/reconstruct.py
  - diagnostics/v1_convergence/check.py
  - diagnostics/v1_convergence/summarize.py
  - docs/DURABLE_WRITES.md
  - notebooks/01_results.ipynb
  - phase3_outputs/estimate_audit_size.py
  - phase3b_outputs/estimate_storage.py
  - phase3b_preflight/estimate.py
  - phase3c_outputs/final_tests.log
  - phase3c_outputs/gate_receipt.json
  - phase3c_outputs/syntax_check.json
  - phase3c_outputs/unit_tests.log
  - phase3c_outputs/verified_ledger_inventory.json
  - scripts/capture_command.py
  - scripts/observe_census_rss.py
  - src/bdr_queue.py
  - src/build_report.py
  - src/census.py
  - src/census_io.py
  - src/data.py
  - src/durable_io.py
  - src/experiment.py
  - src/monitor_census.py
  - src/package_audits.py
  - src/replay.py
  - src/summarize.py
  - src/verify_results.py
  - tests/test_durable_io.py

- Housekeeping warning: one zero-byte unpublished log scratch file remained after completed processes exited. Removed it; no final file was missing. Added a global .writing-* ignore rule so scratch cannot enter commits. This is logged housekeeping, not a scientific stop. Additional files touched: .gitignore, docs/DURABLE_WRITES.md, phase3c_outputs/housekeeping.json.


### Post-3C memory benchmark — integrity stop before iteration probes (2026-09-08)

- Confirmed origin/main and phase3c-complete at 29274b4dc39546c1fed402b928e5fd99d878c1a5; tree matches approved local 07eec35. Started a clean branch from the public tag. Prior gate verified all 5,880 records; no expected digest or recovered artifact was changed.
- User authorized memory measurement first, then the nine high-ceiling iteration probes, with census requiring a further memory decision. Largest retained real fixed_10 census audit: C12/11, 87,782,855 bytes, 2,315 fits, widened scope. Confirmed source digest/size and complete census structure before benchmarking. Derived actual quarter/half prefixes strictly for size scaling, no model fitting or new outcomes generation. Each measurement runs in a fresh process holding the feature matrix and deserialized audit, using the unchanged durable helper; full decoded equality, counts and disk hashes remain enabled.
- Five measurements completed: 21.355 MiB audits peaked at 382.75–382.77 MiB RSS, verification 1.137–1.160 s; 42.735 MiB at 535.16–536.81 MiB, 2.600–2.616 s; first 83.716 MiB full audit at 889.52 MiB, 5.614 s verification (8.401 s write plus verification). Full-size peak is 2.23 times the historical 398 MiB worker figure. Deserialized input loses producer string sharing; figures are conservative for that component and do not measure a complete learner's optimizer state. Warm-cache only, approximately linear size scaling over three sizes, no safe upper-bound claim.
- Available cgroup ceiling 14 GiB, about 1.855 GiB in use at launch. Two observed full-size peaks plus previously measured parent high-water total about 2.29 GiB. Preliminary memory recommendation is retain two workers and all verification, not streaming or reduced checks. No memory mitigation or production concurrency change applied. This is arithmetic based on completed observations, not a concurrent-worker stress test.
- STOP: second full-size repetition failed in the staged full-value verifier with JSONDecodeError at character 70109136, after the preceding staged parse/count returned. Atomic replacement had not occurred; no final output exists. This is a new content-integrity failure, not evidence of out-of-memory and not a passing gate. Existing cleanup removed the failed temporary, so bad bytes are unavailable. Exact cause remains unresolved. No rerun, changed assertion, expected digest update or weakened validation followed. The source audit still matches its committed digest.
- Full outer log and all six child logs inspected; no fit warnings, last child has parsing traceback and parent has CalledProcessError. Successful outputs were independently hashed after process exit. Failure report, raw per-operation timing/RSS receipts, memory provenance, explanatory README and protected-file receipt retained in diagnostics/verification_memory. Large benchmark copies remain ignored in audit_store/verification_memory and are regenerable from the source audit; not scientific results or Git artifacts.
- Prepared diagnostics/iteration_requirements_v2/probe.py to use verified recovered paths and immutable committed expectations, retaining high diagnostic ceiling 1,000,000 and proposed twice-observed-max rounding rule. It was NOT executed. No actual iteration requirements obtained, no production max_iter or convergence-logging changes, no census, no failed-generation reuse/supersession by a new generation, no Phase 4. These remain uncompleted under the integrity stop. src/, results/ and both plans match phase3c-complete.
- Files added in this gate:
  - diagnostics/iteration_requirements_v2/probe.py
  - diagnostics/verification_memory/README.md
  - diagnostics/verification_memory/benchmark.log
  - diagnostics/verification_memory/benchmark.py
  - diagnostics/verification_memory/failure_report.json
  - diagnostics/verification_memory/protected_files.json
  - diagnostics/verification_memory/provenance.json
  - diagnostics/verification_memory/size0_repeat0.json
  - diagnostics/verification_memory/size0_repeat0.log
  - diagnostics/verification_memory/size0_repeat1.json
  - diagnostics/verification_memory/size0_repeat1.log
  - diagnostics/verification_memory/size1_repeat0.json
  - diagnostics/verification_memory/size1_repeat0.log
  - diagnostics/verification_memory/size1_repeat1.json
  - diagnostics/verification_memory/size1_repeat1.log
  - diagnostics/verification_memory/size2_repeat0.json
  - diagnostics/verification_memory/size2_repeat0.log
  - diagnostics/verification_memory/size2_repeat1.log


### Requested write-failure investigation — setup integrity stop (2026-09-08)

- User accepted the memory assessment and retained two workers with full verification; no memory mitigation or reduced check introduced. User authorized independent repeated write trials and size sweeps, with failed bytes retained, while all model probes/census/Phase 4 remain blocked. Read AGENTS.md and prior memory-gate failure.
- Added diagnostics/write_investigation/investigate.py: intended design 20 actual full-size trials, 10 per exact 20/40/60/70/80/100 MiB, five per native helper/plain serializer/tmpfs control. Unique serial child paths, no retry or production writer changes. Child cleanup hook would hard-link failing staged bytes before original unlink, and JSONDecodeError.doc would be retained separately to distinguish stored-file corruption from an inconsistent read buffer.
- Six exact-size diagnostic inputs were prepared with the existing helper. Before any child or rate trial, plan metadata publication failed the unchanged full-value verifier: resource.getrlimit tuples decode as JSON lists. Numeric limits were unchanged, but container types disagree. This is a diagnostic-driver schema error, not the large-artifact failure under investigation. Stopped; did not correct the input shape or alter verification after the failure. A Python import-scoping issue was corrected during setup before any trial or related error occurred; no experimental outcome was affected.
- Capture limitation: the failure-preservation hook was inside the child, so it did not retain the failed setup plan temporary; the helper removed it. No failed audit bytes, byte-70109136 inspection, first-divergence comparison, failure rates, threshold or filesystem root cause established. No extra success assurance claimed for project artifacts. Preliminary disk/limits/OOM/mount observations are recorded in README, not presented as causal exclusions.
- Full investigation.log read: setup traceback only, zero child receipts, zero model fits. Both plans, original results/ and src/ match phase3c-complete. No max_iter determination, census, mitigation or Phase 4. Stop at integrity gate. Files added: diagnostics/write_investigation/{investigate.py,investigation.log,setup_failure.json,protected_files.json,README.md}; CHANGES.md appended. Prepared large inputs remain ignored, without being relabeled as completed trial outputs.


## 2026-09-08 — Authorized tuple fix and large-write investigation (reliability remains BLOCKED)

- Corrected diagnostic metadata, not verification: resource.getrlimit values are lists before serialization. Strict JSON full-value equality, all assertions/count/digest checks and src/durable_io.py remain unchanged. Setup and trial failure capture now preserve files before normal cleanup; parser-rejected buffers are retained separately. Earlier diagnostic files remain historical and unmodified; corrected driver is in diagnostics/write_investigation_v2/.
- New setup exposed an 80 MiB post-publication truncation, retained at 69,953,367 bytes. Did not retry it. Used the previous successfully published size inputs, validated against already-committed digests; recorded the separate setup failure and fixed the 95-trial plan before execution. Also retained cleanup leftovers (including a partial 40 MiB scratch with complete final input); these are not relabeled failed trials.
- Completed all 95 planned independent serial fresh-process trials: full 87,782,855-byte audit 6/20 failures; 20/40/60/70/80/100 MiB respectively 0/10, 0/10, 0/10, 1/10, 3/10, 2/10. Controls: native helper on overlay 1/5, plain serialization comparator 0/5, original wrapper on tmpfs 0/5. Thirteen failures total, retained as failures, no retries. Additional separately planned application-instrumented controls were 0/10 and are reported separately; they change timing and do not replace the primary denominator.
- All 13 failed files and 12 available rejected parser buffers are exact reference prefixes: tail truncation, no observed gap/mid-stream corruption. Twelve trial failures followed successful staged full-value validation and replacement, one occurred at staged validation after a successful staged parse. Missing fsync is therefore not a sufficient explanation. This localizes loss between reads but does not prove a particular sandbox/filesystem actor. Syscall tracing was denied (PTRACE_TRACEME/PTRACE_SETOPTIONS); zero traced trials ran. No escalation, retry, weakened check or production workaround was attempted.
- Smallest failing tested size was 70 MiB, not a proven hard threshold. Size groups were ordered and later trials accumulated cgroup memory reclaim pressure, although early failures occurred with no memory-limit events and low usage. Finite controls cannot exclude a timing interaction or unknown external mutator. Earlier approximately 99 KB ledgers and sub-1 MB PNG contradict the suggestion that all historical losses were large-file-only. No claim that these incidents have one proven root cause.
- Current unchanged-reference rechecks: 125 original results/plan files, 38 completed failed-generation audits, 105 verified original/recovered v1 ledgers and 88 retained Phase 1 repeatability artifacts: zero mismatches. Additionally recomputed 408 phase3c-complete Git blob identities, excluding only the authorized CHANGES append: zero mismatches. Categories overlap. Eight original convergence failures remain historical; no model was fit in this investigation. All 82 primary PASS files and ten API-control PASS files matched after process exit. This supports current checked bytes, not indefinite persistence or correctness of an originally wrong input.
- REPORT.md, rates.json, per-trial receipts/logs, byte-forensic reports, environment/resource evidence, trace denial and API event logs are under diagnostics/write_investigation_v2/. Raw bytes stay in ignored audit_store/write_investigation_v2/ and are supplied as lossless per-case evidence archives with successful references separately. Archive grouping is delivery only, after unchanged full-size trials, never a chunking/streaming writer workaround. Detailed added-file inventory is files_added.txt; no files outside this diagnostic directory and CHANGES.md are changed at this gate.
- Full new investigation logs inspected and indexed in log_inspection.json: thirteen target failures, one setup failure, trace permission denial; no captured new convergence warning. A terminal attempt to read packaging.log before capture_command published it returned missing-file, followed by an accidental invalid shell command; neither ran a model nor changed an artifact/check. These housekeeping errors are not write-trial outcomes. No test-suite rerun is used to claim storage reliability; production code and tests are unchanged.
- Explicitly uncompleted: kernel-level attribution/exclusion of an external actor, a reliable-write clearance for this sandbox, max_iter probes/production iteration-budget changes, census and Phase 4. The investigation is complete; the storage reliability gate is not passed. Recommend investigating the same unchanged writer on the user's machine before authorizing census there; no such run is initiated here. No original expected digest, result, plan or model parameter was revised.
- Gate addendum: all 24 raw-evidence archives independently reopened after producer exit; CRC/member counts and raw-member digests agree with existing forensic/reference records. Total 631,110,332 bytes; individual archives 8,132,268–34,668,414 bytes. The process-listing diagnostic also failed (ps: fatal library error, lookup self), recorded separately; external-process exclusion is explicitly incomplete. Final protected-byte check again matched all 408 phase3c-complete blobs apart from the authorized CHANGES append. New diagnostic Python syntax parsed; no production tests were represented as rerun.


## 2026-09-08 — Accepted storage findings; nine-fit iteration determination; local census preparation

- Confirmed user patch base cbe42024cc632bc1872f18a7ed45313944e48361 on origin/main, with a clean worktree. All handoffs in this phase use this SHA. A later assistant commit is not presumed to be the user's commit; future patches must use the latest user-confirmed SHA.
- Corrections to the user's earlier instructions, explicitly accepted by the user: (1) the claim that all historical losses exceeded 70 MB was false: v1 ledgers were approximately 99 KB and the Phase 1 PNG below 1 MB; (2) the claim that the verification guard always fired before replacement was false: twelve of thirteen trial failures were detected after publication. Historical investigation reports remain immutable snapshots, not rewritten to anticipate these resolutions.
- Two phenomena remain separate. The current size-associated tail loss in this sandbox's overlay storage was reproduced in 13/95 investigation trials (all thirteen failures on overlay, with clean reference prefixes; tmpfs 0/5). This is accepted as sufficient operational evidence to move census off this sandbox; exact storage-layer mechanism is not claimed identified. Earlier small-file losses remain unexplained and are not accounted for by size. No unified size threshold or common proven historical root cause is asserted. Further sandbox filesystem diagnosis is not undertaken.
- Nine high-ceiling fits were authorized in the sandbox because they avoid large audit serialization. Small writes are not infallible given earlier small-file incidents; strict input and post-exit record verification remained enabled. Runtime/source/ledger/audit hashes were checked against committed references. No ground-truth fixture or expected digest changed. The preexisting probe ran before new learner instrumentation. Its original learner is now frozen byte-for-byte from cbe4202 for repeatable future census-warning reconstruction; this import-only preservation does not rerun or alter the measured fits.
- Individual high-ceiling iteration counts: v1 C15/11/uncertainty fit 9 = 17,616; C15/11/fixed_20 fit 209 = 10,190; C15/29/uncertainty fit 40 = 14,607; C15/29/uncertainty fit 47 = 12,322; C15/29/fixed_20 fit 250 = 20,005; C15/47/auto_tar fit 47 = 11,171; C15/47/fixed_20 fit 221 = 10,644; C15/47/fixed_20 fit 247 = 11,704; reconstructed census C15/11/uncertainty fit 47 = 10,959. All nine converged with no high-ceiling warnings. The census locator replayed 47 original-configuration fits and encountered one expected ConvergenceWarning at max_iter=10,000. There was no saved failure-round state for independent identity comparison; the ninth case is the first reproduced warning under the failed-generation config, explicitly not a saved-state match.
- Observed range 10,190–20,005 (spread 9,815, about 1.96-fold). Applied the probe's already-recorded headroom rule ceil(2*max/1000)*1000 = 41,000. Recorded in experiment_plan_v2.json after observing failures. This is a numerical optimization-budget change, not a change to C, loss, dual, tol, objective, sampling or the auto_tar schedule; changed ranking where earlier optimization was incomplete remains possible. It is not a guarantee that every later Mac fit will converge. The original experiment_plan.json remains untouched. The new plan carries the previously authorized 12-policy, 23,149-budget sweep and widened margin rule; direct experiment execution stays disabled, with authorization derived only by the macOS local runner.
- New fit records permanently include n_iter, converged, captured warning category/message, unchanged actual fit parameters and fit seconds. Non-convergence prints to the run log and remains in the audit; fitting/review continues, no automatic limit increase. Missing fit metadata fails production validation. Mocked fits in legacy tests may have no n_iter attribute and are not falsely labeled converged. A full grid with any such warnings is retained as COMPLETE_WITH_NONCONVERGENCE, exit 2, requiring review before analysis; all topic/policy counts include explicit zeros.
- Local runner uses two fresh worker subprocesses, a generation lock inherited by workers, parent-death detection, terminal progress, durable per-worker logs, per-run completion receipts and a generation manifest. Parent waits for full child exit before parsing/counting/hashing against the producer's digest. Resume validates completed audit/receipt pairs and prior manifest entries under identical code/config/source/environment, then skips them. A structurally complete but unreceipted interrupted audit is preserved separately and that run restarts; no expected producer digest is fabricated. Missing or mismatched completed evidence and prior FAILED generations stop for review, with no automatic retry. An interrupted in-progress trajectory is not resumed within a fit.
- macOS preflight checks CPython 3.12.13, all pinned package versions, clean committed source, source-lock identity/digest, full-audit reference digest, source N/R, thread limits, disk space and available RAM. It records native feature and environment fingerprints; resume/worker checks bind to this local fingerprint, not a claim of bitwise Linux/macOS equivalence. 20 GiB free disk and 4 GiB currently available RAM are conservative operational reserves. Native CPU/BLAS differences, Mac package availability and actual wall time remain unverified here. Instructions use a dedicated conda environment; existing Anaconda base packages are not reused. Exact Python availability or pinned-wheel installation failure must stop rather than silently substitute versions.
- Standalone local filesystem command runs the six unchanged Phase 3C fault/roundtrip contracts on the audit volume, then twenty full 87,782,855-byte reference write/verify trials with independent post-exit full-value/digest checks. No fits or retries; failure retains evidence and blocks census. A finite success is not proof of indefinite filesystem reliability. Existing failed-generation audit is used only as an I/O reference, never a new scientific run. Full source/reference input bundle uses unchanged committed digests.
- The 38 completed audits of 20260907_census_18add1f5ba0e are marked superseded for production in that FAILED manifest; entries and expected digests are unchanged. They are not reused by the new generation. Audits and local output logs remain ignored; manifests remain committable. Mac ru_maxrss bytes are normalized to KiB; sampled process-tree RSS and session wall times are reported with their limitations.
- All 27 tests passed: twenty existing contracts unchanged plus seven mocked local orchestration/convergence contracts. Two injected test ConvergenceWarnings are expected and fully logged; the nine measured high-ceiling refits produced none. Existing replay, schedules, strict config, durable publication and assertion expectations were not altered. No macOS run, census or Phase 4 occurred in this environment; native installation, full-size Mac write check and 180-run completion remain gates on the user's machine. Detailed command guide and touched-file inventory accompany the handoff.
- Preparation review caught two resumability details before deployment: the current generated manifest must be permitted as the sole dirty path, and disk checks must credit already-written generation files against the initial 20 GiB allowance (minimum 2 GiB free reserve). Otherwise successful work would prevent resuming. Added a mocked manifest/source-edit contract; 28 tests now pass, with every earlier assertion unchanged. Existing source edits remain a stop. Producer worker RSS is updated after its own audit readback, before the completion receipt.
- Handoff verification: input ZIP is 38,505,039 bytes and contains the locked 3,853,819-byte corpus plus the 87,782,855-byte I/O reference; independent post-exit CRC, member and committed-digest checks passed. Nine fit states and the probe/log/provenance records are supplied separately as a small evidence ZIP. All 132 protected files checked (original results/plan, source lock, durable helper and preexisting tests) are byte-identical to cbe4202. Every preexisting failed-generation manifest field remains unchanged; only explicit supersession fields were added. Original C/loss/dual/tol match the base.
- Local orphan preservation also uses the unchanged durable helper for a fully verified copy before removing the uncommitted source; no alternate serializer/publication path was introduced. The sole new non-artifact writer exception is the operational flock file (no study data). Per-run logs and every application JSON/CSV writer use the Phase 3C helper. Full-size local trials and new production audits never ran here. Package builds read existing evidence, not regenerated outcomes.
- Final gate log inspection covers the 56-line probe log and all three contract-test logs in full. All 28 tests pass at the final tested contract state; subsequent display-only progress and native-library provenance additions were syntax-checked. No expectation/tolerance was changed. Native macOS execution remains explicitly uncompleted, so this is a preparation handoff rather than a completed census phase.
